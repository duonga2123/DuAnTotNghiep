<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Danh Sách Truyện</title>
    <link href='https://unpkg.com/boxicons@2.0.9/css/boxicons.min.css' rel='stylesheet'>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
    <style>
        /* Thêm CSS tùy chỉnh */
        .card {
            border: none; /* Loại bỏ viền */
            border-radius: 0; /* Loại bỏ viền bo tròn */
        }

        .card-img-top {
            width: 100%;
            height: 300px;
            border-bottom: 0; /* Loại bỏ viền dưới hình ảnh */
        }

        /* Độ dài cố định cho các thành phần */
        .card-body {
            height: 250px; /* Điều chỉnh độ dài tùy ý */
            overflow: hidden;
        }
        
    </style>
</head>
<body>

<div class="container mt-4">
    <h2>Danh Sách Truyện</h2>
    <div class="row">
        {{#each manga}}
        <div class="col-sm-6 col-md-4 mb-4">
            <div class="card">
                <img src="{{this.image}}" class="card-img-top" alt="{{this.manganame}}">
                <div class="card-body">
                    <h5 style="width: 100%; height:50px; overflow: hidden;" class="card-title">{{this.manganame}}</h5>
                    <p style="margin-top: 20px;" class="card-text">{{this.author}}</p>
                    <p style="margin-top: 10px;" class="card-text">{{this.category}}</p>
                    <a href="/manga/{{this._id}}/chapters" class="btn btn-primary">Đọc Truyện</a>
                </div>
            </div>
        </div>
        {{/each}}
    </div>
</div>

<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.3/jquery.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
</body>
</html>